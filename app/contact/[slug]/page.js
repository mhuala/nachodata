"use client"
import { useRouter } from 'next/navigation'

const Slug = () => {
    const router = useRouter()
    console.log(router)
  
    return (
        <div>
            Enter
        </div>
    );
}

export default Slug;